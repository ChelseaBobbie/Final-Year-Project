<?php
/**
 * Export to PHP Array plugin for PHPMyAdmin
 * @version 5.2.0
 */

/**
 * Database `survey_data`
 */

/* `survey_data`.`response` */
$response = array(
);

/* `survey_data`.`user` */
$user = array(
);
